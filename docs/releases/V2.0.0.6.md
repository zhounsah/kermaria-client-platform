# Release v2.0.0.6
Date: 2026-08-28
## Application
- tag: `v2.0.0.6`
- application commit: `9f47f2558bb5a2cc4b2b0f880f3ea97456cae1b2`
- scope: WEBPORTAL only
- API-INTERNAL: unchanged from v2.0.0.5 (`a6eefcd09833bb7e2384f5a7694e47a4e6621cd1`)
- SQL migration: none
## Delivered changes
Storefront Lot C:
- dedicated customer-oriented rendering for six priority service pages;
- reduced Storefront top spacing and less oversized hero titles;
- customer-friendly commercial guidance with Billing still authoritative;
- VPN vs Windows remote desktop explanation kept on the VPN service page, with the SEO resource as optional deeper reading;
- `/services/domaines-messagerie` rebuilt as a customer-problem router.
Adaptive diagnostic:
- bounded contexts: `backup`, `remote-access`, `network`, `messaging`, `domain-dns`, `server`, `web-hosting`;
- direct `/diagnostic` remains a general orientation flow;
- unknown contexts fall back safely to the general flow;
- service CTAs pass their diagnostic context through the URL;
- pricing and formula authority remain in Billing V2.
## Validation before release
`npm run validate` completed with exit code 0, including:
- secrets guard;
- lint and TypeScript;
- WEBPORTAL and API builds;
- API smoke tests;
- SEO, UX and public-site-quality contracts;
- adaptive diagnostic contract;
- Billing, formulas, catalog and subscriptions contracts;
- signup, editorial, security, payments and backups contracts.
## WEBPORTAL artifact
- archive: `C:\Users\zhounsah\Documents\Dev\_artifacts\webportal-v2.0.0.6-9f47f25.tar.gz`
- size: `6234281` bytes
- SHA-256: `C0B93D3C84F5D9696FC8F774C5397D90D94E612E144284823AFB54E1C1F9580A`
- manifest Git ref: `v2.0.0.6`
- manifest Git commit: `9f47f2558bb5a2cc4b2b0f880f3ea97456cae1b2`
## Production deployment
Host: `KERMARIA-SRV-12`
Active release:
`/opt/kermaria/releases/20260828-104050-v2.0.0.6-9f47f25`
Rollback release retained:
`/opt/kermaria/releases/20260827-191145-v2.0.0.5-a6eefcd`
Service:
- `kermaria-webportal`: active
- local HTTP `192.168.100.212:3000`: 200
- public `/api/health/ready`: 200
The Node process reports status 143 during a normal systemd stop/restart; this is also visible on earlier releases. The new service started normally and remained active.
## Production smoke tests
Diagnostic:
- `/diagnostic`: 200, general flow;
- all seven valid contexts: 200 with context-specific introduction;
- invalid context: 200 and safe fallback to general flow;
- no mojibake detected in checked HTML.
Priority services:
- `messagerie-professionnelle` -> diagnostic context `messaging`;
- `vpn-entreprise` -> `remote-access`;
- `sauvegarde-externalisee` -> `backup`;
- `unifi` -> `network`;
- `infogerance-vps` -> `server`;
- `hebergement-web` -> `web-hosting`.
All checked service pages returned 200 and kept their canonical URL. `/services/domaines-messagerie` returned 200 with the new customer-oriented page.
## Rollback
If a WEBPORTAL rollback is required:
```bash
sudo ln -sfn /opt/kermaria/releases/20260827-191145-v2.0.0.5-a6eefcd /opt/kermaria/webportal
sudo systemctl restart kermaria-webportal
```
No API or database rollback is required for this release because neither component changed.