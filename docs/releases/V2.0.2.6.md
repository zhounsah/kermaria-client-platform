# Release v2.0.2.6
Date: 2026-09-05
Application commit: `8b448933114a1cbe1a1e0404d5ed338b27378595`
Tag: `v2.0.2.6`
## Scope
Post-audit reliability and storefront cleanup release:
- restored and expanded validation coverage, including hidden/previously-unreached suites;
- hardened Billing V2 MariaDB document/refund tests and concurrent-connection handling;
- aligned public Zachary IT identity and legal contact to `contact@zachary-it.fr`;
- synchronized persisted CMS storefront content through migrations 088-093;
- removed public mojibake and added encoding regression guards;
- aligned public terminology around offers while keeping internal Billing identifiers stable;
- improved public SEO/structured data and storefront content quality;
- removed the public application version from the footer.
## Validation before deployment
The release candidate passed:
- secrets check;
- shared and WEBPORTAL typechecks;
- full WEBPORTAL lint;
- API build;
- WEBPORTAL production build (79 pages);
- API smoke tests;
- Billing unit/integration tests;
- managed-content, public-copy, brand, SEO, UX and public-site-quality contracts.
A disposable database named `kermaria_billing_test_20260903` was created on the existing SRV-06 MariaDB 11.8.6 instance. A full fresh bootstrap of migrations 001 through 093 succeeded. All nine `test:billing:mariadb` suites passed. The disposable database was then removed. No additional MariaDB service or instance was created.
Migration 066 requires temporary `SUPER` when binary logging is enabled because it creates triggers, and `CREATE VIEW` because it creates a view. Those global privileges were removed after the bootstrap validation. The normal database-scoped grant remains the intended steady state.
## Production deployment
### API-INTERNAL / SRV-13
- service: `KermariaApiInternal`;
- deployed application commit: `8b448933114a1cbe1a1e0404d5ed338b27378595`;
- rollback copy: `C:\apps\api-internal-old-20260905-171911`;
- deployed executable SHA-256: `E40174C580FB265C5C22E23AD829E641B53FD0CA6AF6D6F7F444D7855F80F5AC`;
- local and remote executable hashes matched exactly;
- `/health/live`, `/health/ready` and `/ready` returned HTTP 200 after restart.
### WEBPORTAL / SRV-12
- service: `kermaria-webportal`;
- active release: `/opt/kermaria/releases/20260905-154724-v2.0.2.6-8b44893`;
- rollback release retained: `/opt/kermaria/releases/20260902-110249-v2.0.2.5`;
- artifact SHA-256: `F21C9E85D3626A3CE0A43CC2DABFD3F158A5C572DBB5D2F8ABC20AD555974E7F`;
- artifact manifest commit: `8b448933114a1cbe1a1e0404d5ed338b27378595`;
- `.next/cache`: `kermaria-web:kermaria-web`, mode `750`;
- local home, live and ready checks returned HTTP 200.
The systemd journal records the expected `status=143` for the old Node process during restart, followed immediately by a successful start. The new service remained active.
## Production migrations
Production is at migration `093_public_contact_identity_sync`.
Migrations 088-093 synchronize the post-audit public CMS content, canonical public offer wording, mojibake cleanup and Zachary IT public contact identity.
## Production smoke tests
After both application components were deployed:
- API-INTERNAL live/ready/ready -> HTTP 200;
- public `zachary-it.fr` live/ready -> HTTP 200;
- dashboard readiness -> HTTP 200;
- administration login and `/admin/settings` -> HTTP 200;
- all 49 URLs in the public sitemap -> HTTP 200;
- mojibake pages -> 0/49;
- `/diagnostic` -> HTTP 200 with zero known mojibake sequences;
- `/tarifs` and `/services/vps` -> HTTP 200 without the previously exposed internal Billing/provisioning wording;
- legal public content contains `contact@zachary-it.fr` and no longer contains `zhounsah@home.bzh` or the former public `zacharyhounsa.ovh` identity.
## Rollback
API-INTERNAL rollback source:
`C:\apps\api-internal-old-20260905-171911`
WEBPORTAL rollback:
```bash
sudo ln -sfn /opt/kermaria/releases/20260902-110249-v2.0.2.5 /opt/kermaria/webportal
sudo systemctl restart kermaria-webportal
```