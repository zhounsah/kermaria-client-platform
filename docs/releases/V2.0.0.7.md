# Release v2.0.0.7

Date: 2026-08-28

## Application

- tag: `v2.0.0.7`
- application commit: `494c8a4ca8f645e15668b72c97d2bd119e01f8ae`
- scope: WEBPORTAL + API-INTERNAL
- SQL migration: none

## Delivered changes

- final UX/UI polish for the adaptive diagnostic, including semantic progress, responsive behavior, selected/focus states and reduced-motion handling;
- structured administration route `/admin/diagnostic`;
- configurable mappings for the five diagnostic recommendation profiles;
- formula choices loaded from the current public Billing V2 catalog;
- `diagnostic:recommendations` persisted through managed content without a new SQL schema;
- generic managed-content editing for that key redirects to `/admin/diagnostic`;
- API-INTERNAL validates submitted formula codes against the current public Billing V2 catalog before persistence;
- unavailable, removed or unset formulas fall back safely to cadrage/devis;
- Billing V2 remains the sole pricing and commercial authority.

## Validation before release

`npm run validate` completed with exit code 0, including secrets, lint, typechecks, WEBPORTAL/API builds and the complete regression suite.

Additional checks completed successfully:

- `git diff --check`;
- `test:diagnostic`;
- `test:formules`;
- `test:catalog`;
- `test:managed-content`;
- `test:public-site-quality`;
- `test:markdown-anchors`;
- local `/admin/backups`, `/admin/diagnostic` and `/diagnostic?context=backup` smokes -> 200.

## Artifacts

API-INTERNAL:

- archive: `C:\Users\zhounsah\Documents\Dev\_artifacts\api-internal-v2.0.0.7.zip`
- size: `2221742` bytes
- SHA-256: `7EEB41EE15915705426B1CBCB82B729C0C9FBB484FC7CF3A0F0BAD15F417812B`
- target: framework-dependent `win-x64` with AppHost
- embedded `DEPLOYMENT_COMMIT.txt`: `494c8a4ca8f645e15668b72c97d2bd119e01f8ae`

WEBPORTAL:

- archive: `C:\Users\zhounsah\Documents\Dev\_artifacts\webportal-v2.0.0.7-494c8a4.tar.gz`
- size: `6266586` bytes
- SHA-256: `4C5456155C7330F43F2DA1A33D6D1A33A26AA9CA20BC219A04358D06A58063FD`
- manifest Git ref: `v2.0.0.7`
- manifest Git commit: `494c8a4ca8f645e15668b72c97d2bd119e01f8ae`

## Production deployment

API-INTERNAL (SRV-13):

- service: `KermariaApiInternal`;
- active runtime: `C:\apps\api-internal`;
- active commit marker: `494c8a4ca8f645e15668b72c97d2bd119e01f8ae`;
- rollback runtime retained: `C:\apps\api-internal-prev-v2.0.0.7-20260828-190204-a6eefcd`.

WEBPORTAL (SRV-12):

- service: `kermaria-webportal`;
- active release: `/opt/kermaria/releases/20260828-171043-v2.0.0.7-494c8a4`;
- rollback release retained: `/opt/kermaria/releases/20260828-104050-v2.0.0.6-9f47f25`.

No SQL migration or direct SQL write was required for this release. The new diagnostic configuration uses the existing managed-content persistence and seed mechanism.

## Production smoke tests

- API `/health` -> 200;
- API `/health/live` -> 200;
- API `/health/ready` -> 200;
- MariaDB -> `healthy`;
- AD -> `controlled_write`;
- WEBPORTAL local readiness on SRV-12 -> 200;
- public dashboard readiness -> 200;
- `https://zachary-it.fr/` -> 200;
- `/diagnostic` -> 200;
- `/diagnostic?context=backup` -> 200;
- `/diagnostic?context=remote-access` -> 200;
- forged diagnostic context -> 200 with safe general fallback contract retained;
- `/admin/diagnostic` -> 200 through the production administration host;
- `/admin/backups` -> 200 through the production administration host;
- diagnostic general/context HTML checked without mojibake.

## Rollback

API-INTERNAL:

1. stop `KermariaApiInternal`;
2. retain the failed runtime separately;
3. restore `C:\apps\api-internal-prev-v2.0.0.7-20260828-190204-a6eefcd` as `C:\apps\api-internal`;
4. restart the service;
5. verify `/health`, `/health/live` and `/health/ready`.

WEBPORTAL:

```bash
sudo ln -sfn /opt/kermaria/releases/20260828-104050-v2.0.0.6-9f47f25 /opt/kermaria/webportal
sudo systemctl restart kermaria-webportal
```

After either rollback, verify private readiness first and then the public dashboard/admin routes.
