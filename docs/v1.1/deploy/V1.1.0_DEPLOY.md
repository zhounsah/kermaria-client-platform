# Déploiement V1.1.0 — SRV-13 + SRV-12 (runbook combiné)

> Fiche maîtresse : ordre des opérations pour déployer la **V1.1.0** sur les deux
> serveurs. Détail par serveur dans [`SRV-13_LOT3_DEPLOY.md`](SRV-13_LOT3_DEPLOY.md)
> et [`SRV-12_LOT3_DEPLOY.md`](SRV-12_LOT3_DEPLOY.md).

## 0. Ce que contient la V1.1.0

Le tag `v1.1.0` (commit `e6f5840`) réunit **deux travaux** :

1. **Comptes de démonstration et d'essai personnalisés** (V1.1, lots 1 à 3) —
   nouveaux écrans d'administration, provisioning cadré des groupes `GG_DEMO_*`,
   cycle de vie et expiration automatique ;
2. **Remise à plat agentique** (ex-releases 1.0.0.7 et 1.0.0.8) — CGV et politique
   de confidentialité révisées au **03 août 2026**, politique de sauvegarde affichée
   sur les packs publics, scripts KoXo.

> Un correctif de test accompagne la fusion (`0759edd`) : l'assertion du smoke sur les
> CGV attendait encore « 07 juillet 2026 » alors que le contenu semé est passé au
> 03 août. Le message d'erreur (« doit être seedé et lisible en UTF-8 ») était trompeur,
> le fichier étant en UTF-8 valide.

## 1. Paquets

Paquets de référence, construits depuis le tag **`v1.1.1`** (commit `2e7cc5b`) —
**V1.1 complète, lot 4 inclus** :

| Serveur | Archive | Taille | SHA256 |
|---|---|---|---|
| **SRV-13** (api-internal) | `kermaria-api-internal-v1.1.1.zip` | 1,39 Mo | `5726B27EDBA779268A1FD1D551AF41AC336B5C5CDED16E22B6765425186E7F6B` |
| **SRV-12** (webportal) | `kermaria-webportal-v1.1.1.zip` | 6,37 Mo | `8150E0AE0932D49A0A04F2B9F491686B2890C8B9E3C1A4409ED5C57A987726D7` |

Une fois déployé, le pied de page du portail doit afficher **« Version v1.1.1 »** —
c'est le contrôle le plus rapide pour vérifier qu'un serveur porte bien cette
livraison. Toute autre valeur signale un déploiement obsolète.

Les archives antérieures (`…-v1.1.0.zip`, `…-v1.1-lot4.zip`) sont **obsolètes** :
ne les déployez pas.

Vérifier l'empreinte avant décompression :

```powershell
Get-FileHash .\kermaria-api-internal-v1.1.0.zip -Algorithm SHA256
```

## 2. Ordre des opérations

L'ordre n'est pas indifférent : le webportal appelle l'API, et les nouveaux écrans
échoueraient contre une API antérieure.

| # | Étape | Serveur | Référence |
|---|---|---|---|
| 1 | Sauvegarde base + dossiers applicatifs | — | runbook général |
| 2 | **Configuration AD** (étape bloquante) | SRV-13 | fiche SRV-13 §2 |
| 3 | Bascule du binaire api-internal | SRV-13 | fiche SRV-13 §3 |
| 4 | **Migrations** (`031` en attente) | SRV-13 | fiche SRV-13 §1 |
| 5 | Redémarrage + contrôle des logs | SRV-13 | fiche SRV-13 §5 |
| 6 | Bascule du webportal | SRV-12 | fiche SRV-12 §3 |
| 7 | Vérification bout en bout | les deux | §3 ci-dessous |
| 8 | Tâche planifiée d'expiration | SRV-13 | fiche SRV-13 §4 |

## 3. Les deux pièges à ne pas manquer

### 3.1 Configuration AD — bloquante, et à portée globale

Les groupes `GG_DEMO_*` vivent dans `OU=Groupes_TEST`, **hors** de `AD_CLIENTS_OU_DN`.
Le garde-fou d'écriture valide le DN du groupe **et** celui de l'utilisateur.

```powershell
AD_ALLOWED_ROOTS    = '<AD_CLIENTS_OU_DN actuel>;OU=Groupes_TEST,DC=clients,DC=home,DC=bzh'
AD_REQUIRED_OU_ROOT = 'DC=clients,DC=home,DC=bzh'
```

⚠️ Si `AD_REQUIRED_OU_ROOT` reste sur `AD_CLIENTS_OU_DN`, `ConfigurationValid` bascule
à **faux** et **toutes** les écritures AD sont refusées — pas seulement la démo. Et ne
jamais retirer `AD_CLIENTS_OU_DN` de `AD_ALLOWED_ROOTS` (le contrat exige sa présence
exactement une fois). Détail et DN complets : fiche SRV-13 §2.

### 3.2 Deux migrations en attente

Les `036`/`037`/`038` sont déjà appliquées, mais **deux** ne le sont pas :

- `031_backup_policy_public_copy_refresh.sql` — issue de la remise à plat
  (`UPDATE` idempotent sur la description de l'offre `SAVE-PERSO`) ;
- `039_v11_demo_conversion.sql` — lot 4 (colonnes de traçabilité de conversion,
  additive et idempotente).

Il faut donc lancer `--apply-migrations` (bascule temporaire sur
`kermaria_migrator`, `--environment Development`) : les migrations déjà passées
sont ignorées automatiquement.

### 3.3 Conversion essai → réel (lot 4) — configuration facultative

```powershell
DEMO_CONVERSION_TARGET_OU_DN = '<OU de destination des identités converties>'
```

Cette OU doit être couverte par `AD_ALLOWED_ROOTS` (le DN est validé au moment du
déplacement). **Non renseignée, la conversion fonctionne** mais l'étape de
déplacement est neutre : l'identité reste dans `OU=CLI-DEMO` et le cloisonnement
n'est plus porté que par les groupes. À trancher avant d'utiliser le bouton
« Convertir » en production.

## 4. Vérification bout en bout

1. Service SRV-13 démarré, logs sans `AD_CONFIGURATION_INVALID`.
2. Contrôle du contrat de configuration :
   `npm --prefix apps/webportal run test:signup`.
3. Portail accessible ; connexion admin ; entrée **« Comptes démo »** visible.
4. `/admin/demo` charge la liste **et** les 4 profils (3 `showcase` + `trial-ad-koxo`).
   Une liste vide signale que SRV-12 ne joint pas SRV-13.
5. Le bandeau `MockNotice` ne doit **pas** apparaître.
5 bis. Pied de page en **« Version v1.1.1 »**, et colonne « Action » avec le bouton
   « Convertir » sur les comptes d'essai de `/admin/demo` (lot 4).
6. Page publique des offres : la mention de politique de sauvegarde apparaît sur les
   packs contenant `SAVE-PERSO` (apport de la remise à plat).
7. CGV publiées en « Version du : 03 août 2026 ».

## 5. Recette fonctionnelle — après les deux déploiements

Dans cet ordre, impérativement :

1. **`showcase-tpe`** — inerte par garde-fou dur dans le code : aucun AD, aucun KoXo,
   aucun e-mail, aucun paiement, quelle que soit la configuration globale. Valide la
   création, le contenu semé et l'affichage sans le moindre effet réel.
2. **`trial-ad-koxo`** — durée de vie **très courte**. Vérifier l'ajout aux
   `GG_DEMO_*`, puis la révocation (retrait des groupes + désactivation du compte) et
   la purge au balayage suivant.

Codes utiles dans les logs : `DEMO_PROVISIONING_APPLIED`,
`DEMO_PROVISIONING_PENDING_IDENTITY` (identité KoXo pas encore créée — normal, rejoué),
`DEMO_REVOCATION_APPLIED`, `DEMO_REVOCATION_PARTIAL` (réessayé au balayage suivant).

## 6. Retour arrière

Chaque fiche porte sa procédure (bascule du dossier `-old`). Le schéma n'a pas besoin
d'être défait : toutes les migrations sont additives ou idempotentes.
