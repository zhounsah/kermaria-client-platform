# V0.31 - Tests restants provisioning AD des abonnements

Date de reference : 2026-07-07

## Deja valide

- Relance manuelle du provisioning depuis `portal.home.bzh`.
- Provisioning reel du client `CLI-DEMO-0060`.
- Ajout reel de `test.web.0060.user` dans `GG_VPN`.
- Rejeu idempotent avec resultat `PROVISIONING_UNCHANGED`.
- UI admin : bouton de relance visible et correctif du bouton qui restait bloque sur `Relance...`.
- Delegation AD minimale en place pour `HOME\svc_api_portal_ad` sur :
  - `GG_VPN`
  - `GG_RDS`
  - `GG_Radio`

## Tests restants prioritaires

1. Activation automatique Stripe
- Creer une nouvelle souscription Stripe.
- Verifier le passage `pending_activation -> active`.
- Verifier que le provisioning part automatiquement sans relance manuelle.

2. Activation automatique PayPal (OK)
- Creer une nouvelle souscription PayPal.
- Verifier le passage `pending_activation -> active`.
- Verifier que le provisioning part automatiquement sans relance manuelle.

3. Deprovisioning sur fin de souscription (OK)
- Resilier une souscription active.
- Si possible tester aussi `suspended` et `expired`.
- Verifier que le groupe AD est retire seulement si aucune autre souscription active du client ne le demande encore.

4. Cas multi-abonnements
- Donner au meme client `ACCES-VPN` + `ACCES-RDS`.
- Verifier l'union des groupes (`GG_VPN` + `GG_RDS`).
- Resilier une seule offre.
- Verifier que seul le groupe devenu inutile est retire.

5. Mapping complementaire
- Tester reellement le mapping `ACCES-RDS -> GG_RDS`.
- Tester `Radio -> GG_Radio` des qu'une offre catalogue avec `external_reference` compatible existe.

6. Cas d'erreur attendus
- Client sans lien AD `user` : verifier `PROVISIONING_NO_TARGET_USERS`.
- Groupe non configure : verifier l'erreur explicite attendue.
- Relance apres echec : verifier qu'elle reste possible sans casser l'etat metier.

7. Supervision admin
- Verifier l'affichage de la fiche abonnement apres :
  - `PROVISIONING_APPLIED`
  - `PROVISIONING_UNCHANGED`
  - un echec de provisioning

## Ordre conseille pour la reprise

1. Test de nouvelle souscription VPN en automatique.
2. Test de resiliation VPN et verification du retrait de groupe.
3. Test cumule VPN + RDS sur le meme client.

## Contexte

Pause fonctionnelle voulue ici : restructuration du catalogue / des offres en cours.
Le provisioning V0.31 est suffisamment avance pour reprendre les tests plus tard a partir de cette checklist.
